// Function to get random number from 1 to the maximum number set
function getRandomInt(max) {
  return Math.floor(Math.random() * Math.floor(max) + 1);
}

// When calling the function, pass in an argument for the max number that you want the random number to be
getRandomInt(100); 
